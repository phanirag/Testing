package logicsqustions;

import java.util.Arrays;


public class test {
	
	/*	final String min = Arrays.stream(arr).min(String.CASE_INSENSITIVE_ORDER).orElse(null);
	final String max = Arrays.stream(arr).max(String.CASE_INSENSITIVE_ORDER).orElse(null);
	asd pqrs xyz asdf qwerty mnop ab qwerty abb ad asd*/

	public static void main(String[] args) {
			
		String data = "abcadferkae";
		//String arr[] = data.split(" ");
		//int min = arr.length;
		//int max = 0;
		//String maxvalue = null;
		//String minvalue = null;
		/*for(int i=0;i< min;i++){
			if(arr[i].length() < min){
				min = arr[i].length();
				minvalue = arr[i];
			}
			else if(arr[i].length() > min)
			{
				max = arr[i].length();
				maxvalue = arr[i];
			}
		}
		System.out.println("min value is "+min);
		System.out.println("min string is "+minvalue);
		System.out.println("max value is "+max);
		System.out.println("min string is "+maxvalue);
		
		for(int i = 0;i < arr.length ;i++){
			for(int j = 0; j< arr.length ; j++){
				boolean flag= arr[i].contains(arr[j]);
				if(flag == true){
					System.out.println(arr[j]);
				}
			}
		}
	}*/
}}
